# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/user/application_security/sast/#available-cicd-variables
# Secret Detection customization: https://docs.gitlab.com/user/application_security/secret_detection/pipeline/configure/
# Dependency Scanning customization: https://docs.gitlab.com/user/application_security/dependency_scanning/#customizing-analyzer-behavior
# Container Scanning customization: https://docs.gitlab.com/user/application_security/container_scanning/#customizing-analyzer-behavior
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ci/variables/#cicd-variable-precedence
stages:
- test
- secret-detection
- mirror
variables:
  SECRET_DETECTION_ENABLED: 'true'
secret_detection:
  stage: secret-detection
include:
- template: Security/Secret-Detection.gitlab-ci.yml

# Mirror to GitHub on every push to main.
# Requires a GitLab CI/CD variable named GITHUB_TOKEN set to a GitHub
# Personal Access Token (classic) with the "repo" scope.
# Add it at: GitLab → Settings → CI/CD → Variables → Add variable
#   Key:   GITHUB_TOKEN
#   Value: <your PAT>
#   Mask:  yes   Protect: yes
mirror-to-github:
  stage: mirror
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
  image: alpine/git:latest
  script:
    - git remote add github "https://nmadd57:${GITHUB_TOKEN}@github.com/nmadd57/marmot-server.git"
    - git push github "HEAD:main" --force
